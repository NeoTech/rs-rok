[package]
name = "rs-rok-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Binary framing protocol for rs-rok tunnels"

[features]
default = ["std"]
std = []

[dev-dependencies]
